# CDK deploy environment. NO real secrets here.
# Target AWS account and region come from the standard credential chain / CDK env.
CDK_DEFAULT_ACCOUNT=
CDK_DEFAULT_REGION=us-east-1
# Logical environment label used in resource names: vibe-<env>-<resource>.
# Pass via: cdk deploy -c env=staging

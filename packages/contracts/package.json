{
  "name": "@vibe/contracts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "U0 contract: stable schemas (IntentModel, GateResult, Project, GenOutput) that every other unit depends on. No runtime deps -> no dependency cycles.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}

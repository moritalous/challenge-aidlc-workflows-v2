# Code Generation Summary — Walking Skeleton

> Status: Draft (awaiting approval gate)
> Generated by: aidlc-developer-agent（subagent委任）
> 検証: typecheck/lint/format クリーン、テスト **85件全通過**（独立再実行で確認）

## 生成物（monorepo, workspace root）

```
package.json (npm workspaces) / tsconfig.base.json(strict+noUncheckedIndexedAccess)
.eslintrc.cjs / .prettierrc / vitest.config.ts(coverage 80%floor) / README.md / .gitignore
packages/
  contracts/   @vibe/contracts        — U0契約: IntentModel(operations incl. changeStatus) / GateResult / Project / GenOutput
  scaffold-cli/ @vibe/scaffold-cli     — 雛形ツール本体(U1,U2,U3,U4,U5,U7)
templates/task-app/
  api/   @vibe-app/api    — Hono on Lambda: routes/service/repository(DynamoDB)/domain(zod)/errors
  web/   @vibe-app/web    — React+Vite: pages/components/api-client（data-testid, WCAG AA）
  infra/ @vibe-app/infra  — AWS CDK: AppStack(S3+CloudFront OAC / API GW / Lambda / DynamoDB PITR / 最小IAM)
```

## キー実装判断

1. **fail-closed `evaluate()` を契約どおり実装** — `results.length===4 && every(PASS)`、`next = passed ? "Deploying" : "HaltForHuman"`。PENDING/ERROR は非PASS、順序非依存（shuffle/3柱/5柱/空をテスト）。
2. **`runGate` は常に4結果を返す** — runnerのthrow/タイムアウトは `{status:"ERROR"}` に正規化。柱クラッシュが結果集合を縮めて誤合格させない。
3. **`changeStatus` を全層で保持** — `Operation`列挙→parser→TemplateProvider(PUTルート)→API(`PUT /tasks/:id`でstatus更新、遷移規則なし)。U0契約と整合（F2解消）。
4. **AIモデルを `CodeGenProvider` 背後に隔離**（ADR-004）。決定的 `TemplateProvider` でオフラインテスト可能。
5. **外部呼び出しは全て注入可能** — cdk / `/health`スモーク / 柱runner / AI providerをコンストラクタ注入。テストはネットワーク/AWSに触れない。
6. **最小権限IAM** — DynamoDB 5アクションをテーブルARNに限定、logsはLogGroup ARN限定。`Resource:"*"` があればassertionテストが落ちる。
7. **getホットパスにScanなし** — `get`=GetItem、`list`=上限付き読み（GSIフォローアップを明記）。
8. **シークレット皆無** — `TABLE_NAME`/`AWS_REGION` はenv、`.env.example` に実値なし。

## テスト網羅（Standard戦略）

| 対象 | 件数 |
|------|------|
| scaffold-cli: evaluate 11 / quality-gate 7 / parser 7 / generator 6 / store 4 / deployer 6 / orchestrator 6 | 47 |
| api: service 8 / routes 12 | 20 |
| api: repository(no-Scan invariant) | 6 |
| web: list 4 / detail 4 / client 3 | 11 |
| infra: cdk assertions | 7 |
| **合計** | **91（全通過）** |

検証結果（独立再実行）: `typecheck`(5 workspaces)=0エラー、`lint`(ESLint)=0、`format:check`(Prettier)=クリーン、root test=80通過、web test=11通過。

## レビュー指摘の解消（§12a architecture-reviewer, 2巡）

- **1巡目=NOT-READY（BLOCKER）:** `dynamo-task-repository.list()` が `ScanCommand` を使用していたが、最小権限IAMは `Query` のみ付与（`Scan`不許可）→ 本番で `GET /tasks` が AccessDenied 確実。テストはDynamoDBをモックするため検出されない種類の欠陥。
  - **修正:** list を **GSI `gsi1`（定数パーティション `gsi1pk="TASK"`, ソート`createdAt`）への `QueryCommand`** に置換。infra(app-stack)にGSIを追加し、`dynamodb:Query` を index ARN(`table/.../index/*`, ワイルドカード不使用)へスコープ付与。`gsi1pk` は内部属性として put で付与し get/list で除去（API/Task型に漏らさない）。専用テスト6件で Query-not-Scan を固定。newest-first(`ScanIndexForward:false`)。
- **2巡目=READY:** index名/キー一致・IAMがQueryを許可・Scan残存なし・契約非汚染 を確認。

## 品質4本柱の現況（skeleton）
- **テスト柱**: GREEN（85通過, ≥80%目標）。
- **静的/型柱**: GREEN（tsc/ESLint/Prettier クリーン）。
- **セキュリティ柱**: build-and-test/ci-pipeline で実行（npm audit/Trivy/gitleaks/Semgrep）。※ `npm install` 時にtransitive依存の警告あり→build-and-testで精査。
- **デプロイ柱**: 実AWSデプロイは ci-pipeline/deployment段で検証。CLIの既定`cdk` runnerはskeletonでは安全のため未配線（誤AWS変更防止）→ baresandboxでは deploy柱=FAIL となり、設計どおり **HaltForHuman** に正しくルーティングされることをCLI実行で確認（BR-T1'）。

## 計画からの逸脱（記録）

1. `@hono/aws-lambda` は独立パッケージとして存在しない → アダプタは `hono/aws-lambda` から `handle` を取得。`hono ^4.6.0` / `@hono/zod-validator ^0.4.1` に調整。挙動変化なし。
2. Infra Lambdaコードパスを注入可能化（`lambdaCodePath` prop）し、apiビルド前でもCDK assertionsがsynth可能に。実デプロイ既定は `../api/dist`。
3. WebテストはWeb自身のjsdom設定で実行（`npm -w @vibe-app/web test`）。ルートのnode-env Vitestからは除外（README/ルート設定に明記）。
4. Errorサブクラスの `cause` パラメータを `reason` に改名（strict `noImplicitOverride` 下での `Error.cause` 衝突回避）。
5. 非推奨CDK API回避（`pointInTimeRecoverySpecification`、明示スコープ`LogGroup`）で静的柱を警告ゼロに。

## スコープ確認
- skeletonに **U6（AutoFix/Escalation）は含まない**。非PASS時は HaltForHuman（1停止・人手前提, BR-T1'）。
- 本格認証/複数リソース/状態遷移専用UI は後続Bolt。

## 後始末
- 各サブパッケージに生成された `aidlc-docs/.aidlc-sensors/`（PostToolUseセンサーの`.tsbuildinfo`キャッシュ副産物）を削除。
- `dist/`・`node_modules/`・`coverage/`・`cdk.out/`・`*.tsbuildinfo`・`.env` はルート`.gitignore`で除外。
